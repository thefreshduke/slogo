package commands.information;

/**
 * Interface to unify all containers as one specific type. Ideally there would
 * be methods in the interface which entail shared behavior in that on top of
 * being a container, they all have a sense of scope in "active" and "inactive"
 * (i.e. active turtles, active grids, variables associated within a specific
 * command etc.)
 * 
 * @author Duke
 *
 */
public interface IInformationContainer {

}
